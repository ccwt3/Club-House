<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign-up</title>
  <link rel="stylesheet" href="register.css">
</head>
<body>
  <form action="/sign-up" method="post">
    <div class="input__item">
      <label for="name">
        Name:
        <% if(err && err.name) { %>
          <p class="error__message"><%= err.name.msg %></p>
        <% } %>
      </label>
      <input type="text" name="name" id="name">
      
    </div>

    <div class="input__item">
      <label for="last_name">
        Last name: 
        <% if(err && err.last_name) { %>
          <p class="error__message"><%= err.last_name.msg %></p>
        <% } %>
      </label>
      <input type="text" name="last_name" id="last_name">
    </div>

    <div class="input__item">
      <label for="username">
        Username:
        <% if(err && err.username) { %>
          <p class="error__message"><%= err.username.msg %></p>
        <% } %> 
      </label>
      <input type="text" name="username" id="username">
    </div>

    <div class="input__item">
      <label for="password">
        Password: 
        <% if(err && err.password) { %>
          <p class="error__message"><%= err.password.msg %></p>
        <% } %>
      </label>
      <input type="password" name="password" id="password">
    </div>

    <div class="input__item">
      <label for="passwordConfirmation">
        Confirm password: 
        <% if(err && err.passwordConfirmation) { %>
          <p class="error__message"><%= err.passwordConfirmation.msg %></p>
        <% } %>
      </label>
      <input type="password" name="passwordConfirmation" id="passwordConfirmation">
    </div>

    <button type="submit">Register</button>
  </form>
</body>
</html>